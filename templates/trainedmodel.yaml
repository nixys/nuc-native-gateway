{{- range $index, $item := .Values.trainedModels }}
{{- if gt $index 0 }}---{{ end }}
{{ include "nuc-kserve.renderResource" (dict "root" $ "item" $item "resourceKey" (printf "trainedModels[%d]" $index) "defaultApiVersion" $.Values.apiVersions.trainedModel "kind" "TrainedModel" "namespaced" true) }}
{{- end }}
