{{- range $index, $item := .Values.grpcRoutes }}
---
{{ include "nuc-native-gateway.renderResource" (dict
  "root" $
  "item" $item
  "kind" "GRPCRoute"
  "defaultApiVersion" $.Values.apiVersions.grpcRoute
  "resourceKey" (printf "grpcRoutes[%d]" $index)
  "namespaced" true
) }}
{{- end }}
