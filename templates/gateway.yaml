{{- range $index, $item := .Values.gateways }}
---
{{ include "nuc-native-gateway.renderResource" (dict
  "root" $
  "item" $item
  "kind" "Gateway"
  "defaultApiVersion" $.Values.apiVersions.gateway
  "resourceKey" (printf "gateways[%d]" $index)
  "namespaced" true
) }}
{{- end }}
