{{- range $index, $item := .Values.localModelNodes }}
{{- if gt $index 0 }}---{{ end }}
{{ include "nuc-kserve.renderResource" (dict "root" $ "item" $item "resourceKey" (printf "localModelNodes[%d]" $index) "defaultApiVersion" $.Values.apiVersions.localModelNode "kind" "LocalModelNode" "namespaced" false) }}
{{- end }}
