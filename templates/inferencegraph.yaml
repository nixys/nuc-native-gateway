{{- range $index, $item := .Values.inferenceGraphs }}
{{- if gt $index 0 }}---{{ end }}
{{ include "nuc-kserve.renderResource" (dict "root" $ "item" $item "resourceKey" (printf "inferenceGraphs[%d]" $index) "defaultApiVersion" $.Values.apiVersions.inferenceGraph "kind" "InferenceGraph" "namespaced" true) }}
{{- end }}
