{{- range $index, $item := .Values.clusterStorageContainers }}
{{- if gt $index 0 }}---{{ end }}
{{ include "nuc-kserve.renderResource" (dict "root" $ "item" $item "resourceKey" (printf "clusterStorageContainers[%d]" $index) "defaultApiVersion" $.Values.apiVersions.clusterStorageContainer "kind" "ClusterStorageContainer" "namespaced" false) }}
{{- end }}
