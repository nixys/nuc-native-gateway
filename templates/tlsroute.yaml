{{- range $index, $item := .Values.tlsRoutes }}
---
{{ include "nuc-native-gateway.renderResource" (dict
  "root" $
  "item" $item
  "kind" "TLSRoute"
  "defaultApiVersion" $.Values.apiVersions.tlsRoute
  "resourceKey" (printf "tlsRoutes[%d]" $index)
  "namespaced" true
) }}
{{- end }}
