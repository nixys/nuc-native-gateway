{{- range $index, $item := .Values.clusterServingRuntimes }}
{{- if gt $index 0 }}---{{ end }}
{{ include "nuc-kserve.renderResource" (dict "root" $ "item" $item "resourceKey" (printf "clusterServingRuntimes[%d]" $index) "defaultApiVersion" $.Values.apiVersions.clusterServingRuntime "kind" "ClusterServingRuntime" "namespaced" false) }}
{{- end }}
