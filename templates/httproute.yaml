{{- range $index, $item := .Values.httpRoutes }}
---
{{ include "nuc-native-gateway.renderResource" (dict
  "root" $
  "item" $item
  "kind" "HTTPRoute"
  "defaultApiVersion" $.Values.apiVersions.httpRoute
  "resourceKey" (printf "httpRoutes[%d]" $index)
  "namespaced" true
) }}
{{- end }}
