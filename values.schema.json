{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "title": "nuc-native-gateway values",
  "description": "Schema for nuc-native-gateway Helm chart values.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "nameOverride": {
      "type": "string",
      "description": "Override the chart name used in generated labels."
    },
    "commonLabels": {
      "$ref": "#/definitions/stringMap",
      "description": "Labels merged into every rendered resource."
    },
    "commonAnnotations": {
      "$ref": "#/definitions/stringMap",
      "description": "Annotations merged into every rendered resource."
    },
    "apiVersions": {
      "type": "object",
      "description": "Default API version overrides per supported Gateway API kind.",
      "additionalProperties": false,
      "properties": {
        "backendTLSPolicy": {
          "$ref": "#/definitions/nonEmptyString"
        },
        "gatewayClass": {
          "$ref": "#/definitions/nonEmptyString"
        },
        "gateway": {
          "$ref": "#/definitions/nonEmptyString"
        },
        "grpcRoute": {
          "$ref": "#/definitions/nonEmptyString"
        },
        "httpRoute": {
          "$ref": "#/definitions/nonEmptyString"
        },
        "listenerSet": {
          "$ref": "#/definitions/nonEmptyString"
        },
        "referenceGrant": {
          "$ref": "#/definitions/nonEmptyString"
        },
        "tlsRoute": {
          "$ref": "#/definitions/nonEmptyString"
        }
      }
    },
    "backendTLSPolicies": {
      "$ref": "#/definitions/resourceList",
      "description": "List of BackendTLSPolicy resources."
    },
    "gatewayClasses": {
      "$ref": "#/definitions/resourceList",
      "description": "List of GatewayClass resources."
    },
    "gateways": {
      "$ref": "#/definitions/resourceList",
      "description": "List of Gateway resources."
    },
    "grpcRoutes": {
      "$ref": "#/definitions/resourceList",
      "description": "List of GRPCRoute resources."
    },
    "httpRoutes": {
      "$ref": "#/definitions/resourceList",
      "description": "List of HTTPRoute resources."
    },
    "listenerSets": {
      "$ref": "#/definitions/resourceList",
      "description": "List of ListenerSet resources."
    },
    "referenceGrants": {
      "$ref": "#/definitions/resourceList",
      "description": "List of ReferenceGrant resources."
    },
    "tlsRoutes": {
      "$ref": "#/definitions/resourceList",
      "description": "List of TLSRoute resources."
    }
  },
  "definitions": {
    "nonEmptyString": {
      "type": "string",
      "minLength": 1
    },
    "stringMap": {
      "type": "object",
      "default": {},
      "propertyNames": {
        "type": "string",
        "minLength": 1
      },
      "additionalProperties": {
        "type": "string"
      }
    },
    "freeFormObject": {
      "type": "object",
      "description": "Arbitrary Gateway API payload passed through by the chart.",
      "additionalProperties": true
    },
    "resourceItem": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "name"
      ],
      "properties": {
        "name": {
          "$ref": "#/definitions/nonEmptyString",
          "description": "Resource name."
        },
        "namespace": {
          "$ref": "#/definitions/nonEmptyString",
          "description": "Namespace for namespaced resources. Ignored for cluster-scoped kinds."
        },
        "labels": {
          "$ref": "#/definitions/stringMap",
          "description": "Resource-specific labels."
        },
        "annotations": {
          "$ref": "#/definitions/stringMap",
          "description": "Resource-specific annotations."
        },
        "apiVersion": {
          "$ref": "#/definitions/nonEmptyString",
          "description": "Per-resource API version override."
        },
        "spec": {
          "$ref": "#/definitions/freeFormObject",
          "description": "Resource spec rendered as-is."
        },
        "status": {
          "$ref": "#/definitions/freeFormObject",
          "description": "Optional resource status rendered as-is."
        }
      }
    },
    "resourceList": {
      "type": "array",
      "default": [],
      "items": {
        "$ref": "#/definitions/resourceItem"
      }
    }
  }
}
